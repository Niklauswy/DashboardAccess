export const metadata = {
  title: "Registros de Acceso | Dashboard Access",
  description: "Visualiza los registros de conexión y actividad de usuarios en el sistema",
}

export default function LogsLayout({ children }) {
  return children;
}
