export const metadata = {
  title: "Configuración | Dashboard Access",
  description: "Configuración y ajustes del sistema",
}

export default function SettingsLayout({ children }) {
  return children;
}
