export const metadata = {
  title: "Gestión de Usuarios | Dashboard Access",
  description: "Administración y visualización de usuarios del sistema",
}

export default function UsersLayout({ children }) {
  return children;
}
